# Synthetic container-testing library

Java version: 21

This fixture represents a reusable Java library with a root build, a module build, support
containers, CI, and consumer-facing setup documentation.
