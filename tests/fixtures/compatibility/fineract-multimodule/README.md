# Synthetic multi-module service

Java version: 21

This fixture represents a service repository whose root build, child modules, container files,
workflow, and operator-facing documentation repeat the same Java contract.
